# Confirmation

> [!IMPORTANT]
> 本文仅记录 2026-06-29 的历史状态。KG 当前唯一契约已由
> `docs/changes/20260714-092948-knowledge-graph-retrieval-review/` 取代；不得按本文恢复同步抽取、来源推断或旧审核路径。

## 2026-06-29

* 用户确认可以开始知识图谱方向。
* 已按 Trellis 创建任务 `06-29-knowledge-graph-mvp`。
* 已记录当前建议：第一版知识图谱应轻量、可审核、可追溯，优先服务召回增强和客服排查，不直接做重型图谱可视化或复杂 GraphRAG。
* 用户澄清需要查看本地 RAGFlow 仓库 `/home/adam/projects/ragflow`，不是远程资料。
* 已完成本地 RAGFlow GraphRAG 调研，并记录到 `.trellis/tasks/06-29-knowledge-graph-mvp/research/ragflow-knowledge-graph-local.md`。
* 用户提出希望最终做成大厂那种 3D 可视化图谱，并询问是否应先把基础做好再扩展。
* 已完成主流 KG / GraphRAG / 可视化调研，并记录到 `.trellis/tasks/06-29-knowledge-graph-mvp/research/industry-knowledge-graph-visualization.md`。
* 当前建议：第一版先做可信 KG 底座、审核流、证据追溯、检索投影和局部子图 API；3D 可视化作为第三阶段能力，不进入第一版核心实现。
* 用户确认第一版采用“独立审核表 + 确认后投影为 KG chunk，并预留局部子图 API”的路径，3D 可视化放到后续阶段。
* 用户确认第一版实体类型和关系类型采用固定客服领域枚举。
* 用户确认第一版接入最小 KG 检索增强，但仅作为显式开关/评测调试路径，不默认影响所有客服回答。
* 当前范围确认完成；下一步进入实现准备。
* 已按上述确认实现后端第一版底座：KG 候选解析、审核表 schema、候选保存、实体/关系确认投影、默认检索排除 KG、评测 `use_kg` 显式开关、局部子图 API。
* 用户确认下一步先做单来源 KG 抽取任务入口，不做批量全库扫描。
* 已实现 `POST /api/kg/extraction-jobs`，支持从单条 usable FAQ 或单个未禁用文档切片同步抽取 KG 候选，并写入 job 状态。
* 已继续实现 KG 审核后端 API：实体/关系列表、确认投影、状态更新；当前仍未实现审核 UI。
* 已确认本次不实现 UI/3D；后续 UI 需先走布局讨论和 UI prompt 流程。
* 用户提供 KG 页面参考图，并强调必须符合本项目既有页面结构约束，不能做成常驻三栏。
* 已确认右侧详情应做成抽屉：主页面保持左侧导航 + 中间单列表，点击实体/关系后右侧抽屉展示详情、证据、局部关系和审核动作。
* 用户确认“直接一直做，最后让我验收前端+后端”。
* 已实现 KG 前端审核工作台 `/knowledge-graph`：实体/关系列表、状态和类型筛选、当前页搜索、AI 抽取入口、右侧详情抽屉、证据和审核动作。
* 已保留 3D 可视化为后续阶段；当前前端只消费局部子图 API 做抽屉内邻接关系展示。
* 2026-07-01 用户安排另一个 agent 对 KG MVP 做代码 review，并修复了一批潜在 bug。
* 2026-07-02 已复查 Trellis 进度：主任务仍为 `in_progress`，工作树开始时 clean；review 修复已进入提交历史。
* 2026-07-02 已清理 review 修复后残留的 ruff 未使用变量/导入，并完成前后端质量门复验。
* 2026-07-02 用户指出文档 ID / 切片 ID 不好获取，导致 KG 抽取体验不顺。
* 2026-07-02 已确认并实现更顺的路径：文档抽屉可复制文件 ID，切片工具栏可复制切片 ID，并从当前切片一键触发 KG 抽取，无需手动去 KG 页面填切片 ID。
* 2026-07-02 用户指出切片工具栏过挤，`页 14–15 · text` 中的 `text` 没有明确价值，KG 页面 FAQ/文档切片下拉意义不大。
* 2026-07-02 已确认并实现精简路径：切片页码使用 `p14-15`，不显示 block type；切片工具栏只显示 `复制ID` + icon，完整 ID 通过 hover 和 toast 提供；KG 抽取弹层只输入来源 ID，由后端自动识别 FAQ 或文档切片。
* 2026-07-06 继续收尾 KG MVP：已将 PRD 验收项更新为完成状态，清理旧 `customer_service_agent` 路径记录，修复 KG/文档前端 `page_start=0` 页码展示边界，并完成前后端质量门验证。
