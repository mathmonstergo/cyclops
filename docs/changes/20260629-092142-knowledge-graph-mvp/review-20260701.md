# Knowledge Graph MVP — Code Review (2026-07-01)

> [!IMPORTANT]
> This is a historical review snapshot. The only current KG contract is documented in
> `docs/changes/20260714-092948-knowledge-graph-retrieval-review/`; do not reintroduce synchronous extraction or inferred source locators from this report.

## Scope

`git diff HEAD` — 17 files, +1520/-118 lines

## Review Method

max effort (10 角度 × 8 candidates → 1-vote verify → sweep → ≤15 findings)

## Findings

### 🔴 Confirmed Bugs (8)

| # | Severity | File | Line | Summary |
|---|----------|------|------|---------|
| 1 | **High** | `customer_service_agent/db/kg.py` | 128 | `confirm_kg_entity` crashes on nonexistent entity_id — `fetchone()` returns None, passed to `build_kg_entity_knowledge_chunk_row` which accesses `.get('aliases')` → `AttributeError`, returns 500 not 404 |
| 2 | **High** | `customer_service_agent/db/kg.py` | 139 | `confirm_kg_relation` crashes on nonexistent relation_id — same pattern, `row['id']` raises `TypeError: 'NoneType' object is not subscriptable` |
| 3 | **Medium** | `customer_service_agent/db/kg.py` | 173 | `set_kg_entity/relation_status` to `'usable'` skips projection creation if entity was never confirmed — no `knowledge_chunks` row exists, UPDATE hits 0 rows, entity marked usable but never searchable |
| 4 | **Medium** | `customer_service_agent/db/kg.py` | 224 | `get_kg_subgraph` `hops` parameter completely ignored — `_kg_subgraph_sql` has no `%(hops)s` placeholder, always returns 1-hop only |
| 5 | **Medium** | `customer_service_agent/kg_ai.py` | 58 | `_strip_json_fence` regex `\{.*?\}` non-greedy match stops at first nested `}`, truncating JSON → extraction fails when model wraps output in fences |
| 6 | **Medium** | `customer_service_agent/admin_server.py` | 1030 | `create_kg_extraction_job`: if `save_kg_extraction_candidates` succeeds but final `update_kg_extraction_job(status='completed')` fails, data is persisted but job stuck at `processing` |
| 7 | **Medium** | `customer_service_agent/admin_server.py` | 1022 | Synchronous LLM call blocks HTTP thread for 5-30s; codebase already has SSE async pattern (`iter_import_generation_events`) not reused |
| 8 | **Low** | `customer_service_agent/db/kg.py` | 563 | `_set_kg_projection_status_sql` uses f-string for `source_type` instead of parameterized placeholder — latent SQL injection if future caller passes user input |

### 🟡 Plausible (2)

| # | Severity | File | Line | Summary |
|---|----------|------|------|---------|
| 9 | **Low** | `customer_service_agent/retrieval.py` | 117 | `getattr(doc, 'id')` without default crashes on dict input; no current caller passes dicts but signature accepts `Any` |
| 10 | **Low** | `customer_service_agent/kg_ai.py` | 49 | `page_start=0` is falsy, dropped by `or` short-circuit, losing page info for cover/zero-indexed pages |

### 🔵 Code Quality (5)

| # | File | Line | Summary |
|---|------|------|---------|
| 11 | `customer_service_agent/kg.py` | 321 | `_clean_int`, `_clean_list`, `_content_hash`, `_join_search_text` duplicate existing utilities in `db/builders.py` |
| 12 | `customer_service_agent/kg.py` | 57-160 | `build_kg_entity_knowledge_chunk_row` and `build_kg_relation_knowledge_chunk_row` ~75% copy-paste |
| 13 | `customer_service_agent/db/kg.py` | 55-72 | `save_kg_extraction_candidates` does 56 individual INSERTs; use batch insert instead |
| 14 | `customer_service_agent/db/kg.py` | 98-99 | `list_kg_entities` / `list_kg_relations` use two separate queries (COUNT + SELECT); use `count(*) OVER()` |
| 15 | `customer_service_agent/db/kg.py` | 168-186 | `set_kg_entity_status` and `set_kg_relation_status` identical logic; extract common method |

## Fix Priority

1. **Immediate**: #1, #2 — None checks in confirm methods
2. **Immediate**: #3 — Auto-create projection on status→usable if missing
3. **Soon**: #4, #5 — Fix hops SQL / JSON fence regex
4. **Soon**: #6 — Wrap final update in error handling
5. **Nice-to-have**: #7-#15

## Verification

All findings verified by independent agent review of actual code. Each confirmed finding was checked against the real implementation with concrete trigger conditions identified.
